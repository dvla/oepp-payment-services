Payment Services
========

Project provides following components of Payment Service Layer:

 * payment-domain (see: [README](payment-domain))
 * payment-service (see: [README](payment-service))

It also provides some components needed for integration with payment broker/ provider:

 * payment-broker-domain (see: [README](payment-broker-domain))
 * payment-provider-stub (see: [README](payment-provider-stub))
